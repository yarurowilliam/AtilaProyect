import { FiltroMisProyectosPipe } from './filtro-mis-proyectos.pipe';

describe('FiltroMisProyectosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMisProyectosPipe();
    expect(pipe).toBeTruthy();
  });
});
