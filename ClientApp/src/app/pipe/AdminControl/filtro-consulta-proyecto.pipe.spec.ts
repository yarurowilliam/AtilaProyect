import { FiltroConsultaProyectoPipe } from './filtro-consulta-proyecto.pipe';

describe('FiltroConsultaProyectoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroConsultaProyectoPipe();
    expect(pipe).toBeTruthy();
  });
});
