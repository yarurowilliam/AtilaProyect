import { FiltroConsultaDocentePipe } from './filtro-consulta-docente.pipe';

describe('FiltroConsultaDocentePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroConsultaDocentePipe();
    expect(pipe).toBeTruthy();
  });
});
