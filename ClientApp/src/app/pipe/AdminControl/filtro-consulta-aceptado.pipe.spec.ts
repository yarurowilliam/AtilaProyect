import { FiltroConsultaAceptadoPipe } from './filtro-consulta-aceptado.pipe';

describe('FiltroConsultaAceptadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroConsultaAceptadoPipe();
    expect(pipe).toBeTruthy();
  });
});
