import { FiltroProyectoCalificacionPipe } from './filtro-proyecto-calificacion.pipe';

describe('FiltroProyectoCalificacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProyectoCalificacionPipe();
    expect(pipe).toBeTruthy();
  });
});
