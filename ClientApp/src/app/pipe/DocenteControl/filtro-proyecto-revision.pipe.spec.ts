import { FiltroProyectoRevisionPipe } from './filtro-proyecto-revision.pipe';

describe('FiltroProyectoRevisionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProyectoRevisionPipe();
    expect(pipe).toBeTruthy();
  });
});
