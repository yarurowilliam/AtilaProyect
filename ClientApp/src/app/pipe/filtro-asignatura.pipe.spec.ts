import { FiltroAsignaturaPipe } from './filtro-asignatura.pipe';

describe('FiltroAsignaturaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAsignaturaPipe();
    expect(pipe).toBeTruthy();
  });
});
